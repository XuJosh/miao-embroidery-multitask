# 实验评估标准

本文采用训练集、验证集与测试集三划分策略评估模型性能。训练过程中，以验证集上的总损失最小作为模型选择标准；测试阶段报告各任务的准确率与 AUC。

## 1. 数据集划分与模型选择

数据集按照训练集（train）、验证集（val）与测试集（test）进行划分：

- **训练集（train）**：用于模型参数更新。
- **验证集（val）**：用于早停与模型选择。
- **测试集（test）**：仅在最终评估时使用，不参与训练与调参。

在优化阶段，本文以验证集上的总损失作为模型选择依据：

$$
\theta^* = \arg\min_{\theta} \mathcal{L}_{\text{total}}(\theta; D_{\text{val}})
$$

其中：

- $\theta$：模型参数；
- $D_{\text{val}}$：验证集；
- $\mathcal{L}_{\text{total}}$：动态多任务总损失。

即选择验证集总损失最小的模型作为最终模型，再于测试集上报告性能。

## 2. 评估指标

本文涉及真伪鉴别、纹样分类与疵点检测三个任务。根据任务特性，采用**准确率（Accuracy）**与**受试者工作特征曲线下面积（AUC）**作为评估指标。

### 2.1 准确率（Accuracy）

对于单标签分类任务，准确率定义为预测正确的样本数占总样本数的比例。设 $N$ 为测试样本总数，$y_i$ 为第 $i$ 个样本的真实标签，$\hat{y}_i$ 为模型预测标签，则：

$$
\text{Accuracy} = \frac{1}{N} \sum_{i=1}^{N} \mathbb{1}(y_i = \hat{y}_i)
$$

其中：

- $N$：测试样本总数；
- $y_i$：第 $i$ 个样本的真实标签；
- $\hat{y}_i = \arg\max(p_i)$：第 $i$ 个样本的预测标签，即模型输出 logits $p_i$ 中概率最大类别对应的标签；
- $\mathbb{1}(\cdot)$：指示函数，当括号内条件成立时取 1，否则取 0。

本文分别报告三个任务的准确率：

- **Auth Acc**：真伪鉴别准确率；
- **Pat Acc**：纹样分类准确率；
- **Def Acc**：疵点检测准确率（仅适用于 `cls` 模式）。

### 2.2 受试者工作特征曲线下面积（AUC）

AUC 衡量模型区分正样本与负样本的能力，对类别不平衡情况下的二分类任务更为稳健。对于真伪鉴别与疵点检测两个二分类任务，本文同时报告 AUC。设正样本预测概率为 $\{p_i^+\}$，负样本预测概率为 $\{p_j^-\}$，则 AUC 可表示为：

$$
\text{AUC} = \frac{1}{N^+ N^-} \sum_{i=1}^{N^+} \sum_{j=1}^{N^-} \mathbb{1}(p_i^+ > p_j^-)
$$

其中：

- $N^+$：正样本数量；
- $N^-$：负样本数量；
- $p_i^+$：第 $i$ 个正样本的预测概率；
- $p_j^-$：第 $j$ 个负样本的预测概率。

AUC = 1 表示完全区分，AUC = 0.5 表示随机猜测。本文报告 **Auth AUC**（真伪鉴别 AUC）与 **Def AUC**（疵点检测 AUC）。

### 2.3 疵点分割交并比（IoU）

在疵点分割（`defect_mode = seg`）模式下，采用像素级交并比（Intersection over Union, IoU）评估疵点定位精度。设预测二值掩码为 $\hat{M}$，真实掩码为 $M$，则单张图像的 IoU 为：

$$
\text{IoU} = \frac{|\hat{M} \cap M|}{|\hat{M} \cup M|}
$$

其中：

- $\hat{M}$：由模型输出的像素级概率图经阈值 0.5 二值化得到的预测掩码；
- $M$：真实疵点掩码；
- $|\hat{M} \cap M|$：预测掩码与真实掩码的交集像素数；
- $|\hat{M} \cup M|$：预测掩码与真实掩码的并集像素数。

最终 **Def IoU** 为测试集所有图像 IoU 的平均值。

## 3. 指标汇总

| 指标符号 | 适用任务 | 含义 |
|---|---|---|
| Auth Acc | 真伪鉴别 | 预测真伪类别正确的样本比例 |
| Auth AUC | 真伪鉴别 | 真伪二分类的 ROC 曲线下面积 |
| Pat Acc | 纹样分类 | 预测纹样类别正确的样本比例 |
| Def Acc | 疵点检测（cls） | 预测是否有疵点的样本正确比例 |
| Def AUC | 疵点检测（cls） | 疵点二分类的 ROC 曲线下面积 |
| Def IoU | 疵点分割（seg） | 预测疵点掩码与真实掩码的交并比 |

所有实验均在相同的数据划分与随机种子下进行，以保证结果的可比性。最终报告为测试集上评估得到的指标，并辅以混淆矩阵与分类报告进行误差分析。
