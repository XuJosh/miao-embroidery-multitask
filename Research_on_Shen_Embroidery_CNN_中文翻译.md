# 基于卷积神经网络的沈绣文化遗产创新应用研究

**作者**：朱嘉俊（Jiajun Zhu）、朱长永（Changyong Zhu）  
**单位**：南通理工学院 计算机与信息工程学院  
**期刊**：*Scientific Reports*，2024，14:9574  
**DOI**：10.1038/s41598-024-60121-7  
**许可证**：Creative Commons Attribution 4.0 International License（开放获取）

---

## 摘要

为了保护非物质文化遗产并弘扬优秀文化作品，本文开展了基于卷积神经网络（CNN）的沈绣创新应用研究。研究对沈绣数据集进行了预处理与扩充，引入迁移学习对识别网络进行微调，并采用空间金字塔池化（Spatial Pyramid Pooling，SPP）替换 MobileNet V1 网络中的全局平均池化层，实现了局部特征与全局特征的融合。实验结果表明，改进后的 MobileNet V1 识别准确率达到 **98.45%**，比基线 MobileNet V1 网络提高了 **2.3%**。实验证明，改进后的卷积神经网络能够高效识别沈绣，为非物质文化遗产的智能化发展提供了技术支撑。

**关键词**：沈绣；卷积神经网络；MobileNet V1；迁移学习；创新研究

---

## 1. 引言

沈绣是中华优秀传统工艺与现代纺织业融合的杰出代表，起源于中国近代纺织工业发源地——江苏南通，其创始人为著名刺绣艺术家沈寿。然而，随着社会发展，现代工业对传统手工艺的冲击使沈绣传统技艺的传承面临严峻挑战。因此，采用多种手段识别、保护并代代相传沈绣这一非物质文化遗产，变得极为迫切。

长期以来，国内对沈绣的保护与传承主要依赖人工识别、挑选与分类，这些工作劳动强度大、耗费大量人力与财力。随着计算机处理技术的发展和计算机视觉技术的出现，计算机能够自动学习并理解照片、视频等视觉数据。图像分类技术可根据图像的特征与视觉特性将其划分为不同类别。借助计算机视觉技术，可以有效地自动学习并区分沈绣，从而更好地识别不同类型的刺绣。

尽管传统机器视觉技术取得了一系列成果，但通常需要人工标注图像中的关键特征。由于不同标注者对图像的理解和描述存在差异，关键词标注也会不一致，导致分类方法的准确率和效率受标注者主观因素影响较大，效果不佳。

随着技术进步和人工智能的兴起，卷积神经网络（CNN）得到了进一步发展。研究人员已将 CNN 应用于图像识别、目标检测、语义分割等领域，并渗透到生活、生产、医疗等各个方面。目前，CNN 已有多种架构和变体，如 VGGNet、ResNet、Xception 等，这些模型在图像分类精度上显著提升，并在许多分类任务中取得了成功。在非物质文化遗产保护领域，研究人员也开始将深度学习技术引入文化传承与保护中。

本研究针对沈绣作品数量有限、数据集匮乏、人工挑选分类过程繁琐等问题，将人工智能技术应用于沈绣识别，以辅助研究者更好地研究沈绣，进一步保护与传承非物质文化遗产。本文主要工作如下：

1. 通过图像处理技术对数据集进行增强与扩充；
2. 实验并对比五种不同的图像分类网络，进行分析和讨论；
3. 引入迁移学习对分类网络进行微调，并分析讨论；
4. 用空间金字塔池化（SPP）替换 MobileNet V1 中的平均池化层，并分析精度提升效果。

---

## 2. 材料与方法

### 2.1 数据集与实验环境

本研究的实验数据集主要来源于江苏省南通市沈绣博物馆，并通过网络爬虫补充部分沈绣图像，以扩充数据集。数据集共包含 **1264 张** 沈绣图像，统一尺寸为 224×224 像素，格式为 JPG。

实验前，采用翻转、旋转、颜色变换等数据增强方法对数据集进行扩充，平均每张图像增强 **15 次**，共得到 **18,960 张** 增强后的沈绣图像。数据集按 **9:1** 划分为训练集和验证集，训练集用于网络训练，验证集用于学习过程中的自检验证。此外，还收集了包含 **100 张** 图像的独立测试集（其中 80 张为沈绣图像，20 张为非沈绣图像），用于评估训练后模型的性能。

实验平台基于 Windows 10 操作系统，在 Anaconda 环境下使用 Python 语言编程，深度学习框架为 PyTorch。主要硬件配置包括 8 GB 显存的 GeForce GTX 1070Ti GPU 和 AMD Ryzen 5 1600X 六核处理器。网络训练 epoch 数为 **200**，批量大小（batch size）为 **4**，初始学习率为 **0.001**，输入图像尺寸为 224×224，每训练 5 个 epoch 保存一次模型。

### 2.2 MobileNet V1 网络

MobileNet V1 是一种面向移动设备的轻量级神经网络，具有模型体积小、参数量少、计算速度快的特点。本文采用 MobileNet V1 进行沈绣识别。该网络共 28 层，使用带步长的卷积代替池化层进行下采样，并采用深度可分离卷积（depthwise separable convolution）和批归一化（BN）层。网络结构由标准卷积层（Conv Std）、13 个深度卷积层（Conv dw）、13 个点卷积层（Conv pw）、平均池化层（avg Pool）和全连接层（FC）组成。网络中使用步长为 2 的深度卷积替代池化操作，同时保留末尾的全局平均池化层；每个卷积层后均接 BN 层和 ReLU 激活函数。

### 2.3 迁移学习

训练卷积神经网络通常需要大量图像数据，而沈绣图像的纹理特征（如形状、颜色）不够明显，需要高保真图像作为数据集以更好地提取关键纹理特征。然而，获取充足的训练数据困难且标注成本高。AlexNet、ResNet、Xception 等 CNN 模型已在大规模 ImageNet 数据集上预训练，能够迁移到不同任务而无需从头训练，有助于网络泛化并加速收敛。

模型微调是指解冻预训练模型的顶层，使学习到的特征更贴合当前任务。将已训练模型迁移到新任务并继续训练，即为迁移学习。鉴于沈绣数据有限，本研究采用迁移学习以避免训练参数过多、降低网络过拟合风险。

### 2.4 空间金字塔池化（SPP）

空间金字塔池化（SPP）模块借鉴了空间金字塔思想，能够实现局部特征与全局特征的融合，增强特征图的表达能力，有利于提升图像中目标尺寸变化较大情况下的检测精度。在本研究构建的识别网络中，SPP 模块设置在特征输出层之前，由三个并行的最大池化分支（池化核大小分别为 1×1、3×3、5×5）和一个拼接（concatenate）操作组成。SPP 模块的输入为卷积后得到的 7×7 特征图，输出为三个分支拼接后的结果。

### 2.5 评价指标

本文采用准确率（Accuracy，ACC）作为模型评价指标，公式如下：

\[
ACC = \frac{TP + TN}{TP + FP + FN + TN}
\]

其中，TP 表示正确识别为“沈绣”的样本数；TN 表示正确识别为“非沈绣”的样本数；FN 表示将“沈绣”误判为“非沈绣”的样本数；FP 表示将“非沈绣”误判为“沈绣”的样本数。

---

## 3. 实验结果

### 3.1 MobileNet V1 的训练结果

本研究使用 MobileNet V1 模型进行沈绣图像识别实验，共训练 200 个 epoch，每 5 个 epoch 保存一次检查点。训练过程中，损失曲线（train loss 为红色，validation loss 为绿色）随 epoch 增加逐渐趋于平稳。大约在 100 个 epoch 后，曲线开始趋于平缓，损失值最终稳定在约 0.1 左右，但接近稳定过程中仍有一定波动。

### 3.2 改进后 MobileNet V1 的训练结果

本研究对 MobileNet V1 模型进行了改进，使用相同数据集输入改进后的网络进行训练，同样训练 200 个 epoch。训练损失曲线显示，随着 epoch 增加，损失逐渐趋于稳定；100 个 epoch 后曲线达到稳定状态，模型拟合良好，损失值最终稳定在约 0.12。

---

## 4. 讨论

### 4.1 不同模型的实验对比

本研究对 AlexNet、VGG16、ResNet50、MobileNet V1 和 InceptionV3 五种主流分类模型进行了横向对比。实验中保持训练数据集、epoch 数、学习率、批量大小和计算机硬件一致。

五种模型训练过程中的损失曲线显示，MobileNet V1 收敛速度最快、波动最小、训练表现最佳；在验证集上，MobileNet V1 同样表现良好。各模型准确率如表 2 所示：

| 模型 | 准确率（%） |
|---|---|
| AlexNet | 88.23 |
| VGG16 | 86.16 |
| MobileNet V1 | 96.15 |
| InceptionV3 | 92.38 |
| ResNet50 | 93.35 |

MobileNet V1 准确率达到 **96.15%**，高于其他四种模型，因此被选为本研究沈绣识别的基线网络。

在包含 100 张图像（80 张沈绣、20 张非沈绣）的测试集上，五个模型均能较准确地识别沈绣。混淆矩阵结果表明，MobileNet V1 正确识别了 98 张图像，仅误判 2 张沈绣图像，对“沈绣”与“非沈绣”的区分能力更强，因此被选为本实验的基线模型。

### 4.2 迁移学习前后的对比

由于沈绣艺术作品数量有限、图像数据集不足，识别率较低。本研究引入迁移学习，在训练过程中加载 MobileNet V1 的预训练权重，保持数据集、参数和实验设备不变。实验结果如图 10 和表 3 所示：

| 模型 | 准确率（%） |
|---|---|
| MobileNet V1 | 96.15 |
| MobileNet V1（迁移学习） | 97.26 |

迁移学习后的 MobileNet V1 平均识别准确率为 **97.26%**，比未使用迁移学习的模型提高了 **1.11%**。以第 4 张非沈绣图像为例，未使用迁移学习时识别为“非沈绣”的置信度为 55.9%，而迁移学习后置信度提升至 95.9%，说明迁移学习后的模型能够更准确地识别非沈绣图像。

### 4.3 改进后 MobileNet V1 的识别结果

为进一步提升沈绣识别准确率，本研究对 MobileNet V1 进行了改进：在迁移学习的基础上，将网络末尾的平均池化层替换为 SPP 模块。使用相同数据集训练后，改进模型拟合后的识别准确率达到 **98.45%**。

在 100 张测试图像上的识别结果表明，改进后的 MobileNet V1 几乎能完美识别沈绣图像，并且对非沈绣图像也能正确判断为“非沈绣”。与原始 MobileNet V1 相比，改进模型在正确识别沈绣和非沈绣图像时均表现出更高的置信度。综合对比实验证实，针对沈绣图像的创新应用，本文提出的基于卷积神经网络的改进 MobileNet V1 模型能够有效识别沈绣图像。

---

## 5. 结论

本研究提出了一种基于卷积神经网络的改进 MobileNet V1 模型，用于识别南通非物质文化遗产——沈绣。本文实验并对比了五种图像分类模型，最终选定 MobileNet V1 作为沈绣识别网络。针对沈绣作品数量少、识别率低的问题，研究对实验数据集进行了增强；在训练过程中引入迁移学习以加速模型拟合；最后将 MobileNet V1 网络中的平均池化层替换为空间金字塔池化（SPP），以更好地提取沈绣特征。

实验结果表明，改进后的 MobileNet V1 识别准确率达到 **98.45%**，比原始网络提高了 **2.3%**，验证了该模型能够准确识别沈绣。沈绣的创新性研究有助于推动人工智能技术在非物质文化遗产保护与传承中的应用。

---

## 6. 数据可用性

本研究生成和/或分析的数据集可在合理请求下向通讯作者获取。部分数据集可在 https://www.scidb.cn/en/s/JR7ZJn 获取。

---

## 备注

- 本文翻译基于 PDF 提取文本整理，图表与参考文献列表详见原始论文 PDF。
- 原文中分类标签“shenxiu”指“沈绣”，“fei”指“非沈绣/非沈绣图像”。

---

## 7. 论文创新点提炼

### 7.1 研究对象创新：将深度学习首次系统应用于沈绣识别

- 沈绣是江苏南通特有的国家级非物质文化遗产，以往主要依靠人工识别、分类和保护，效率低、成本高且受主观因素影响大。
- 本研究将卷积神经网络（CNN）引入沈绣这一具体非遗门类，构建了端到端的沈绣图像自动识别系统，为传统刺绣文化的数字化保护与智能传承提供了新思路。

### 7.2 数据集构建创新：小样本条件下的数据增强策略

- 原始数据仅 1264 张沈绣图像，属于典型的小样本问题。
- 研究采用翻转、旋转、颜色变换等多种增强手段，将数据集扩充至 **18,960 张**（平均每张增强 15 次），并按 9:1 划分训练/验证集，另设 100 张独立测试集，有效缓解了数据不足带来的过拟合风险。

### 7.3 模型选型创新：面向移动端的轻量级网络优选

- 在 AlexNet、VGG16、ResNet50、InceptionV3、MobileNet V1 五种主流网络中，MobileNet V1 在收敛速度、损失波动和识别准确率（96.15%）上综合表现最优。
- 选择 MobileNet V1 作为基线模型，兼顾了识别精度与移动端/轻量化部署需求，更适合文化遗产数字化展示的实际应用场景。

### 7.4 训练策略创新：迁移学习解决小样本训练难题

- 鉴于沈绣图像数量有限，研究加载 ImageNet 预训练权重对 MobileNet V1 进行微调，而不是从头训练。
- 迁移学习使模型准确率从 **96.15%** 提升至 **97.26%**，同时加快了收敛速度，显著提升了模型对“非沈绣”图像的判别置信度。

### 7.5 网络结构创新：SPP 替换 avg pool 实现多尺度特征融合

- 在 MobileNet V1 末端用 **空间金字塔池化（SPP）** 替换传统的全局平均池化层，通过 1×1、3×3、5×5 三种尺度的最大池化并行提取并拼接特征。
- SPP 能够同时捕获局部细节与全局结构信息，使模型对尺寸、纹理变化较大的沈绣图像具有更强的特征表达能力。
- 改进后模型准确率达到 **98.45%**，比基线 MobileNet V1 提高 **2.3 个百分点**，比迁移学习版再提高约 **1.2 个百分点**。

### 7.6 应用价值创新：为非遗智能化保护提供可复制范式

- 研究不仅在技术上实现了高准确率的沈绣自动识别，更重要的是探索了“AI + 非物质文化遗产”的创新应用模式。
- 该方案可为其他传统手工艺、刺绣、织物纹理等文化遗产的数字化采集、自动分类、智能展示与教育传播提供参考和技术支撑。

---

**一句话总结**：本研究在沈绣这一具体非遗领域，创新性地构建了“数据增强 + 迁移学习 + MobileNet V1 + SPP 多尺度特征融合”的轻量级图像识别方案，实现了 98.45% 的高准确率，为非物质文化遗产的智能化保护提供了有效范例。
